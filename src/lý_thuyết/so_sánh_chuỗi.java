package lý_thuyết;

public class so_sánh_chuỗi {
}
//int compara = a.compareTo(b);
//compareTo() là phương thức trong Java giúp
// so sánh hai chuỗi dựa trên thứ tự từ điển (lexicographical order).

//equals() trong String kiểm tra nội dung của chuỗi.
// System.out.println(sb1.equals(sb2)); // false (vì so sánh địa chỉ bộ nhớ)